import { View } from "react-native";
import { Text } from "@/components/shared/ui";

export default function HomeScreen() {
	return (
		<View className="w-full h-full justify-center items-center">
			<Text>Calendar Event Scheduling App</Text>
		</View>
	);
}
